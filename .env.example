# AI Service (Server-side only)
GEMINI_API_KEY=<your_key>

# Appwrite Configuration (Client-side safe)
NEXT_PUBLIC_APPWRITE_PROJECT_ID=<your_project_id>
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_DATABASE_ID=<your_database_id>

# SECURITY NOTE: Never expose API keys with NEXT_PUBLIC_ prefix
# API keys should only be used in server-side code (API routes, server components)
APPWRITE_API_KEY=<your_api_key>  # Only if needed for server-side operations
